name: Deploy Backend Lambda

on:
  push:
    branches:
      - main
      - dev
  workflow_dispatch:

env:
  AWS_REGION: ${{ env.AWS_REGION }}
  AWS_ACCOUNT_ID: ${{ env.AWS_ACCOUNT_ID }}
  ENVIRONMENT: ${{ github.ref_name }}

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: ${{ github.ref_name }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Build Lambda deployment package
        run: |
          echo "📦 Building Lambda deployment package..."
          
          # Create package directory
          mkdir -p package
          
          # Install dependencies
          pip install -r requirements.txt -t package/ --upgrade
          
          # Copy function code
          cp lambda_function.py package/
          
          # Create zip file
          cd package
          zip -r ../lambda_function.zip . -q
          cd ..
          
          echo "✅ Package built: $(du -h lambda_function.zip | cut -f1)"

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Update Lambda function (Primary Region)
        run: |
          FUNCTION_NAME="multiregion-${{ env.ENVIRONMENT }}-api"
          echo "🚀 Updating Lambda function $FUNCTION_NAME in us-east-1..."
          aws lambda update-function-code \
            --function-name $FUNCTION_NAME \
            --zip-file fileb://lambda_function.zip \
            --region us-east-1
          
          echo "⏳ Waiting for function to be updated..."
          aws lambda wait function-updated \
            --function-name $FUNCTION_NAME \
            --region us-east-1
          
          echo "✅ Lambda updated in us-east-1"

      - name: Update Lambda function (Secondary Region)
        run: |
          FUNCTION_NAME="multiregion-${{ env.ENVIRONMENT }}-api-2"
          echo "🚀 Updating Lambda function $FUNCTION_NAME in us-west-2..."
          aws lambda update-function-code \
            --function-name $FUNCTION_NAME \
            --zip-file fileb://lambda_function.zip \
            --region us-west-2
          
          echo "⏳ Waiting for function to be updated..."
          aws lambda wait function-updated \
            --function-name $FUNCTION_NAME \
            --region us-west-2
          
          echo "✅ Lambda updated in us-west-2"

      - name: Deployment summary
        run: |
          echo "✅ Backend Lambda deployed successfully!"
          echo "📦 Package size: $(du -h lambda_function.zip | cut -f1)"
          echo "🌍 Primary (us-east-1): multiregion-${{ env.ENVIRONMENT }}-api"
          echo "🌍 Secondary (us-west-2): multiregion-${{ env.ENVIRONMENT }}-api-2"
          echo ""
          echo "Lambda functions are now running with updated code!"
